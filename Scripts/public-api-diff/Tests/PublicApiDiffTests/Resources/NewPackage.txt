// swift-tools-version:5.7
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let package = Package(
    name: "Adyen",
    defaultLocalization: "en-us",
    platforms: [
        .iOS(.v15)
    ],
    products: [
        .library(
            name: "Adyen",
            targets: ["Adyen"]
        ),
        .library(
            name: "NewLibrary",
            targets: ["Adyen", "TargetWithBinaryDependency"]
        ),
    ],
    targets: [
        .target(
            name: "Adyen",
            path: "Adyen"
        ),
        .target(
            name: "TargetWithBinaryDependency",
            dependencies: [
                .target(name: "Adyen"),
                .target(name: "BinaryDependency")
            ],
            path: "TargetWithBinaryDependency"
        ),
        .binaryTarget(
            name: "BinaryDependency",
            path: "Path/To/Binary.xcframework"
        ),
    ]
)
